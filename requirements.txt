# System dependencies (install via apt):
# sudo apt install python3-gi gir1.2-ayatanaappindicator3-0.1
#
# The venv uses --system-site-packages to access these.

# For real-time file watching (optional but recommended)
inotify
